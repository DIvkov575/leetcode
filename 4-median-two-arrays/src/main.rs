fn main() {
    let a = vec![1,2,3,45];
    let b = vec![3,23,33,45];
}

pub fn find_median_sorted_arrays(nums1: Vec<i32>, nums2: Vec<i32>) -> f64 {
    let median_index = (nums1.len() + nums2.len())/2;

}